 #include<stdio.h>
char wt2ch(float 2wt, float min, float max) {
if (wt2 > max || wt2 < min) return ‘F’;
else return ‘T’;
 }

/* This is the driver function. */
 int main() {
 /* Checks the range. */
 float max=100, min=10;
 /* Checks the range. */
  int fun(int name,char n2, char 3r);
  /* Checks the range. */
  int GUun(int name,char n2, char 3r);

  int GUun(int name,char n2, char 3r);
   int fun(int name,char n2, char 3r);





 
char WT_check();
printf(“Wt is in range: %c”, WT_CHK(67, min, max));
}